<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>MagicNumber:SqldelightSetDataSource.kt$100.0</ID>
    <ID>MagicNumber:SqldelightSetDataSource.kt$4</ID>
    <ID>MagicNumber:SqldelightSetDataSource.kt$4.0</ID>
    <ID>MagicNumber:SqldelightSetDataSource.kt$80.0</ID>
    <ID>SpreadOperator:SqlDelightVariationDataSource.kt$SqlDelightVariationDataSource$( *variations.map { variation -&gt; combine( setQueries.getOneRepMaxForVariation(variation.id, Instant.DISTANT_FUTURE) .asFlow().mapToOneOrNull(dispatcher), setQueries.getEMaxForVariation(variation.id, Instant.DISTANT_FUTURE) .asFlow().mapToOneOrNull(dispatcher), setQueries.getMaxRepsForVariation(variation.id, Instant.DISTANT_FUTURE) .asFlow().mapToOneOrNull(dispatcher), ) { orm, volume, reps -&gt; Variation( id = variation.id, name = variation.name, notes = variation.notes, bodyWeight = variation.body_weight == 1L, favourite = variation.favourite == 1L, lift = Lift( id = variation.lift_id, color = variation.lift_color?.toULong(), name = variation.lift_name, ), oneRepMax = orm?.let { it.toDomain() }, eMax = volume?.let { it.toDomain() }, maxReps = reps?.let { it.toDomain() }, ) } }.toTypedArray() )</ID>
    <ID>SpreadOperator:SqldelightExerciseDataSource.kt$SqldelightExerciseDataSource$( *variationSets.map { exercise -&gt; combine( setQueries.getOneRepMaxForVariation( exercise.variation_id, Instant.DISTANT_FUTURE ).asFlowOneOrNull(dispatcher), setQueries.getEMaxForVariation( exercise.variation_id, Instant.DISTANT_FUTURE ).asFlowOneOrNull(dispatcher), setQueries.getMaxRepsForVariation( exercise.variation_id, Instant.DISTANT_FUTURE ).asFlowOneOrNull(dispatcher), ) { orm, volume, reps -&gt; Triple( exercise.exercise_variation_id, exercise.exercise_id, Variation( id = exercise.variation_id, name = exercise.variation_name, notes = exercise.variation_notes, favourite = exercise.variation_is_favourite == 1L, lift = Lift( id = exercise.lift_id, color = exercise.lift_color?.toULong(), name = exercise.lift_name, ), oneRepMax = orm?.toDomain() ?.copy(bodyWeightRep = exercise.variation_is_body_weight?.let { it == 1L }), eMax = volume?.toDomain() ?.copy(bodyWeightRep = exercise.variation_is_body_weight?.let { it == 1L }), maxReps = reps?.toDomain() ?.copy(bodyWeightRep = exercise.variation_is_body_weight?.let { it == 1L }), bodyWeight = exercise.variation_is_body_weight == 1L, ) ) } }.toTypedArray() )</ID>
    <ID>UnusedPrivateProperty:SqldelightExerciseDataSource.kt$SqldelightExerciseDataSource$private val variationQueries: VariationQueries</ID>
  </CurrentIssues>
</SmellBaseline>
