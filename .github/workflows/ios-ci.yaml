name: iOS CI/CD

on:
  push:
    branches: [ maestro-ci ] # still running this on commit as I test all the time on android. Will update to nightly if this becomes too much
#    paths:
#      - 'androidApp/**'
#      - 'core/**'
#  schedule:
#    - cron: '5 0 * * *' # run after midnight every day (run after midnight to avoid clashing with promotion
#  workflow_dispatch:

env:
  LIFT_BRO_ADMOB_APP_ID: ${{ secrets.LIFT_BRO_ADMOB_APP_ID }}
  LIFT_BRO_AD_UNIT_ID: ${{ secrets.LIFT_BRO_AD_UNIT_ID }}

jobs:
  build_and_deploy_ios:
    runs-on: macos-latest

    steps:

      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: 17

      - name: Download and Install Maestro CLI
        run: |
          curl -Ls "https://get.maestro.mobile.dev" | bash
          echo "PATH=$PATH:$HOME/.maestro/bin" >> $GITHUB_ENV
        shell: bash

      - name: Set up Ruby 3.3
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.3
          bundler-cache: true

#      - name: Install cocoapods
#        run: |
#          gem install cocoapods
#          cd iosApp
#          pod install --repo-update
#        env:
#          LANG: en_US.UTF-8

#      - name: Run linter
#        run: fastlane lint
#
      - name: Run tests
        run: bundle exec fastlane ios test


#      - name: Set up iOS Simulator
#        run: |
#          SIMULATOR_NAME="iPhone 16 Pro"
#          DEVICE_TYPE_ID="com.apple.CoreSimulator.SimDeviceType.iPhone-16-Pro"
#          RUNTIME_ID="com.apple.CoreSimulator.SimRuntime.iOS-18-5"
#
#          SIMULATOR_UDID=$(xcrun simctl list devices | grep "$SIMULATOR_NAME" | awk -F '[()]' '{print $2}')
#          if [ -z "$SIMULATOR_UDID" ]; then
#            SIMULATOR_UDID=$(xcrun simctl create "$SIMULATOR_NAME" "$DEVICE_TYPE_ID" "$RUNTIME_ID")
#            echo "Created simulator with UDID: $SIMULATOR_UDID"
#          else
#            echo "Simulator '$SIMULATOR_NAME' already exists with UDID: $SIMULATOR_UDID"
#          fi
#          xcrun simctl boot "$SIMULATOR_UDID" || true
#          echo "SIMULATOR_UDID=$SIMULATOR_UDID" >> $GITHUB_ENV
#        shell: bash

      - name: Run Fastlane iOS Maestro Lane
        run: bundle exec fastlane run_maestro_ios_tests
        env:
          FL_IOS_PROJECT_DIR: ios # If your Fastfile for iOS is in a sub-directory
          SIMULATOR_UDID: ${{ env.SIMULATOR_UDID }} # Pass UDID to Fastlane
          # Add any other Fastlane-specific environment variables here

      - name: Restore Fastlane Match Keys
        run: |
          echo "${{ secrets.MATCH_GIT_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Build and Submit to App Store (Release Lane)
        env:
          # Same essential Fastlane secrets as above
          FASTLANE_MATCH_PASSWORD: ${{ secrets.FASTLANE_MATCH_PASSWORD }}
          MATCH_GIT_PRIVATE_KEY: ${{ secrets.MATCH_GIT_PRIVATE_KEY }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
          APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
          APPSTORE_PRIVATE_KEY: ${{ secrets.APPSTORE_PRIVATE_KEY }}
          # Any other environment variables
        run: |
          bundle install
          bundle exec fastlane ios deploy
        working-directory: ./fastlane # Or ./iosApp/fastlane if your Fastfile is there

      - name: Upload Build Artifact (IPA)
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: build/artifacts/iosApp.ipa # Adjust path to where gym outputs the IPA
          if-no-files-found: ignore